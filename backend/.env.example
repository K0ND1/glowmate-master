# Database
# Dla lokalnego developmentu (bez Dockera):
DATABASE_URL="postgresql://glowmate_admin:changeme@localhost:5432/glowmate_db"

# Dla Dockera (odkomentuj jeśli używasz docker-compose):
# DATABASE_URL="postgresql://glowmate_admin:changeme@postgres:5432/glowmate_db"

# JWT Secret (ZMIEŃ NA PRODUKCJI!)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Node Environment
NODE_ENV="development"

# Email Configuration (Resend)
RESEND_API_KEY="re_..."
RESEND_FROM="onboarding@resend.dev"

# Application URL
# Point this to your API domain including version prefix if used in email templates
APP_URL="https://api.glowmate.tech/v1"